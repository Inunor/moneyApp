{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna bootstrap",
    "clean": "lerna run clean",
    "test": "lerna run test",
    "project-hook": "lerna run project-hook",
    "prepare": "husky install"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "husky": "^7.0.0"
  }
}
