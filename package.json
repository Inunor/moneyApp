{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npx lerna run build",
    "clean": "npx lerna run clean",
    "test": "npx lerna run test",
    "prepare": "husky install",
    "pre-commit-hook": "npx lerna run pre-commit-hook"
  },
  "devDependencies": {
    "husky": "^7.0.0",
    "lerna": "^4.0.0"
  }
}
